// ! Bu araç @Kraptor123 tarafından | @Cs-GizliKeyif için yazılmıştır.
version = 2

cloudstream {
    authors     = listOf("kraptor")
    language    = "en"
    description = "Erothots Huge Free Place To Find Leaked onlyfans models Videos And images, Models, Pornstars, Gifs ..."

    /**
     * Status int as the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
    **/
    status  = 1 // will be 3 if unspecified
    tvTypes = listOf("NSFW")
    iconUrl = "https://t2.gstatic.com/faviconV2?client=SOCIAL&type=FAVICON&fallback_opts=TYPE,SIZE,URL&url=https://erothots.is/&size=128"
}