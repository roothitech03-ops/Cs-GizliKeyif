// ! Bu araç @Kraptor123 tarafından | @Cs-GizliKeyif için yazılmıştır.
version = 1

cloudstream {
    authors     = listOf("kraptor")
    language    = "en"
    description = "Missed a show? OnScreens turns live cam broadcasts into an HD replay library from Chaturbate, Cam4, Stripchat, Bongacams &#38; more. Stream or download replays free."

    /**
     * Status int as the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
    **/
    status  = 1 // will be 3 if unspecified
    tvTypes = listOf("NSFW")
    iconUrl = "https://www.google.com/s2/favicons?sz=128&domain=www.onscreens.me"
}